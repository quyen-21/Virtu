# VirtuSpace Hybrid Layout AI V2

Run:
```bash
pip install -r requirements.txt
bash start.sh
```

Endpoint: `POST /api/ai/layout/generate`

FE render `response.items`: `modelUrl, x, y, z, rotationY, footprint.widthM, footprint.depthM, footprint.heightM`.
